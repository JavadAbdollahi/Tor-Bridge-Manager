# Tor-Bridge-Manager
An advanced, portable CLI tool to manage, test (TCP Ping), and connect to Tor Bridges using obfs4. Features auto-latency sorting and GeoIP detection. No Python installation required.
